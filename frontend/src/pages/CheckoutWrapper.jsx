import React from "react";
import Checkout from "./Checkout";

/**
 * CheckoutWrapper - Simple wrapper for Checkout component.
 * Order placement and navigation is handled directly in Checkout.jsx
 */
const CheckoutWrapper = () => {
  return <Checkout />;
};

export default CheckoutWrapper;
