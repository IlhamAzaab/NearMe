const RealtimeNotificationListener = () => {
  // Disabled intentionally: generic global new-order banner removed for all roles.
  // Role-specific websocket notifications remain active in their own connectors/listeners.
  return null;
};

export default RealtimeNotificationListener;
